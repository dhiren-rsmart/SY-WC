@using smART.ViewModel;
@using smART.MVC.Present.Helpers;

@model AddressBook

@if (Model != null)
{
    @(Html.Telerik().DropDownListFor(o => o.ID)
        .BindTo(AddressBookHelper.AddressBookList(Model.ID))
        .HtmlAttributes(new { style = "width: 90%" })
        )
}
